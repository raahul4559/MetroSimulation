# Indiranagar — 3D asset folder

Representative station for: **ELEVATED** (single line).

No `model.glb` / `environment.glb` yet — renders via the viaduct procedural renderer
(`components/station3d/environments/ElevatedEnvironment.tsx`) parameterized from
`metadata.json` / `stationConfigs.ts`. Drop `model.glb` + `environment.glb` (+ `textures/`) here to
upgrade to HIGH fidelity — no code changes required.
