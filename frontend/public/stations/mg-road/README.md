# MG Road — 3D asset folder

Representative station for: **UNDERGROUND** (single line).

No `model.glb` / `environment.glb` yet — renders via the tunnel-shell procedural renderer
(`components/station3d/environments/UndergroundEnvironment.tsx`) parameterized from
`metadata.json` / `stationConfigs.ts`. Drop `model.glb` + `environment.glb` (+ `textures/`) here to
upgrade to HIGH fidelity — no code changes required.
