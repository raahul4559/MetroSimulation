# RV Road — 3D asset folder

Representative station for: **ELEVATED** + **INTERCHANGE**.

No `model.glb` / `environment.glb` yet — renders via the elevated procedural renderer plus the
interchange transfer walkway and multi-line concourse sign, parameterized from `metadata.json` /
`stationConfigs.ts`. Drop `model.glb` + `environment.glb` (+ `textures/`) here to upgrade to HIGH
fidelity — no code changes required.
