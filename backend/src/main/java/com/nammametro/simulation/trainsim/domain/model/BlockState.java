package com.nammametro.simulation.trainsim.domain.model;

/** A block section's occupancy: the ground truth a {@link Signal}'s {@link SignalAspect} is derived from. */
public enum BlockState {
    FREE,
    RESERVED,
    OCCUPIED
}
